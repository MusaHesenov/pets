import 'package:Nestcare/service/info/body.dart';
import 'package:flutter/material.dart';

class InoStore extends StatelessWidget {
  const InoStore({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Body(),
    );
  }
}